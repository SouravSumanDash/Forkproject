public class Main
{
	public static void main(String[] args) {
	    int[] input = new int[]{12, 35, 1, 10, 34, 1};
	    
	    //Expected ans is 34.
	    int  secondLargestValue = getSecondLargest(input);
		System.out.println(secondLargestValue);
		
	
	}

	
	static int getSecondLargest(int[] input){
	    
	    int first = input[0];
	    int secondLargestValue = Integer.MIN_VALUE;
	    
	   for(int num : input){
	       if(num > first) {
	           secondLargestValue = first;
	           first = num;
	       }
	       else if (num> secondLargestValue && num != first) {
	           secondLargestValue = num;
	       }
	   }
	   return secondLargestValue;
	}
}
	   
	   
	   
	   There is a class Student which has id and name. IN main class we create 2 student objects. First object has 
	   Sourav 1  Sourav 1. I created a hashmap. key is student value would be string. when i put map.put, object1 CSE and object2 CSE.IT
	   
	   
	   SELECT 
	      B.title,
	      SUM(O.quantity) AS total_copies_sold
	    FROM
	     Books AS B
	    JOIN
	      Orders as O ON B.book_id = O.book_id
	      GROUP BY
	      B.title
	     ORDER BY
	     total_copies_sold DESC
	   LIMIT 5;
	   
	   
